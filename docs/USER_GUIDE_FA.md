

# 📘 راهنمای کاربر - ماشین حساب پیشرفته

به **ماشین حساب پیشرفته** خوش آمدید! این راهنما به شما کمک می‌کند تا از تمامی امکانات قدرتمند این برنامه استفاده کنید.

---

## 🚀 ۱. شروع کار

### نصب برنامه (PWA)
این برنامه یک وب‌اپلیکیشن پیشرونده (PWA) است. می‌توانید آن را برای استفاده آفلاین نصب کنید.
- **موبایل (iOS/Android):** گزینه "Share" (در iOS) یا منو (در اندروید) را بزنید و **"Add to Home Screen"** را انتخاب کنید.
- **دسکتاپ (Chrome/Edge):** روی آیکون نصب در نوار آدرس کلیک کنید.

### نمای کلی
- **نوار بالا:** تغییر سریع **زبان** (EN/FA) و **تم** (تاریک/روشن). آیکون تنظیمات.
- **تغییر حالت (Mode):** تب‌هایی برای انتخاب بین **استاندارد**، **مهندسی**، **ماتریس**، **آمار** و...
- **نمایشگر:** نمایش فرمول و نتیجه.
    - **میکروفون:** روی آیکون بزنید و محاسبات را بگویید (مثلاً "دو به اضافه دو").
    - **کپی:** برای کپی کردن نتیجه، روی متن آن ضربه بزنید.

---

## 🧮 ۲. حالت‌ها (Modes)

### الف. حالت استاندارد (Standard)
مناسب برای محاسبات روزمره.
- **عملیات:** جمع، تفریق، ضرب، تقسیم.
- **ریاضی بصری:** استفاده از نمادهای `×` و `÷` برای خوانایی بهتر.
- **تاریخچه:** محاسبات به صورت خودکار ذخیره می‌شوند.

### ب. حالت مهندسی (Scientific)
برای محاسبات پیشرفته ریاضی.
- **مثلثات:** `sin`, `cos`, `tan` (پشتیبانی از درجه **DEG** و رادیان **RAD**).
- **توابع:** لگاریتم (`log`, `ln`)، ریشه (`√`, `∛`)، توان (`x²`, `xⁿ`).
- **ثابت‌ها:** `π` (پی)، `e` (عدد اویلر).

### ج. حالت مبدل (Converter)
تبدیل واحدهای مختلف اندازه‌گیری.
۱. یک **دسته** را انتخاب کنید (طول، وزن، سرعت و...).
۲. **مقدار** را در کادر سمت چپ وارد کنید.
۳. واحدهای **مبدأ** و **مقصد** را انتخاب کنید.
۴. **دکمه جابجایی:** برای معکوس کردن تبدیل استفاده کنید.
۵. **ذخیره:** برای ثبت تبدیل در تاریخچه، دکمه "ذخیره در تاریخچه" را بزنید.

### د. حالت نمودار (Graphing)
رسم توابع ریاضی با موتور قدرتمند داخلی.
۱. تابع خود را بر حسب `x` وارد کنید (مثال: `x^2` یا `sin(x)`).
۲. نمودار به صورت خودکار بروز می‌شود.
۳. **جابجایی (Pan):** روی نمودار کلیک کنید و بکشید تا جابجا شود.
۴. **بزرگنمایی (Zoom):** با استفاده از اسکرول موس یا جسچر دو انگشتی زوم کنید.

### ه. حل معادلات (Solver)
حل مسائل جبری، هندسی و فیزیک.
۱. تب مورد نظر را انتخاب کنید (خطی، درجه ۲، دستگاه معادلات، فیثاغورس و...).
۲. مقادیر معلوم را وارد کنید.
۳. دکمه **حل معادله** را بزنید. نتیجه و فرمول کامل در تاریخچه ذخیره می‌شود.

### و. ماشین حساب ماتریس (Matrix - جدید ۳.۱)
انجام عملیات جبر خطی.
۱. **تنظیمات:** ابعاد ماتریس A و B را مشخص کنید (تا ۵ در ۵).
۲. **ورودی:** اعداد را در خانه‌های جدول وارد کنید.
۳. **عملیات:**
   - **A + B / A - B:** جمع و تفریق ماتریس‌ها.
   - **A × B:** ضرب ماتریسی (ستون A باید برابر سطر B باشد).
   - **Det / Inv:** محاسبه دترمینان یا معکوس ماتریس A.
   - **Trans:** ترانهاده ماتریس A.

### ز. آمار و احتمال (Statistics - جدید ۳.۱)
تحلیل داده‌های آماری.
۱. **ورودی:** اعداد را با کاما یا فاصله جدا کنید (مثال: `10, 20, 5, 8.5`).
۲. **محاسبه:** دکمه محاسبه را بزنید.
۳. **نتایج:** مشاهده میانگین، میانه، مد، واریانس، انحراف معیار و...

### ح. ارز و زمان (نسخه ۳.۰)
- **ارز:** نرخ لحظه‌ای ارزها. انتخاب ارز پایه و مقدار.
- **زمان:** ساعت جهانی و ابزار مقایسه زمانی.

---

## ⚙️ ۳. تنظیمات و شخصی‌سازی

با کلیک روی آیکون چرخ‌دنده ⚙️ وارد تنظیمات شوید.

- **زبان:** انگلیسی / فارسی (پشتیبانی کامل از راست‌چین).
- **رنگ تم:** انتخاب بین بنفش، آبی، سبز، نارنجی یا صورتی.
- **سبک طراحی (Design Style):**
  - **مدرن:** رابط کاربری تمیز و گرد.
  - **رترو:** سبک پیکسلی و قدیمی با صدای بیپ.
  - **سایبرپانک:** تم نئونی و تیره با کنتراست بالا.
  - **مینیمال:** تخت، بدون سایه و ساده.
  - **نئوبروتالیسم:** سایه‌های تند و خطوط ضخیم.
  - **شیشه‌ای:** حالت مات و شفاف.
- **مدیریت داده:**
    - **خروجی گرفتن:** ذخیره تاریخچه و تنظیمات به صورت متنی (JSON) در کلیپ‌بورد.
    - **وارد کردن داده:** بازیابی اطلاعات با پیست کردن متن JSON ذخیره شده.

---

## 📝 ۴. نکات و ترفندها
- **ورودی صوتی:** مطمئن شوید دسترسی میکروفون به مرورگر داده شده است.
- **پشتیبانی از کیبورد:** از کیبورد فیزیکی خود استفاده کنید! `Enter` برای محاسبه، `Esc` برای پاک کردن.
- **چسباندن (Paste):** می‌توانید عبارات ریاضی را مستقیماً در برنامه پیست کنید (مثلاً "2+2").

---
*توسعه داده شده توسط علی فـ. هرندی*